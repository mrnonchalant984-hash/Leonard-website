import{NextResponse}from"next/server";import{getSession,isAdminSession}from"@/lib/auth";import{prisma}from"@/lib/prisma";export async function GET(){const s=await getSession();if(!isAdminSession(s))return NextResponse.json({error:"Admin required"},{status:403});const[users,jobs,pendingPayments,premiumUsers,transactions]=await Promise.all([prisma.user.count(),prisma.job.count({where:{status:"OPEN"}}),prisma.payment.count({where:{status:"PENDING"}}),prisma.user.count({where:{isPremium:true}}),prisma.transaction.count()]);return NextResponse.json({users,jobs,pendingPayments,premiumUsers,transactions})}
