import {NextResponse} from"next/server"; import bcrypt from"bcryptjs"; import {prisma} from"@/lib/prisma"; import {signToken} from"@/lib/auth";
export async function POST(r:Request){const {email,password}=await r.json(); const u=await prisma.user.findUnique({where:{email:email?.toLowerCase()}}); if(!u||!await bcrypt.compare(password||"",u.passwordHash))return NextResponse.json({error:"Invalid email or password"},{status:401}); const token=signToken({id:u.id,email:u.email,role:u.role,fullName:u.fullName}); const res=NextResponse.json({user:{id:u.id,fullName:u.fullName,email:u.email,role:u.role}}); res.cookies.set("leonardx_token",token,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:604800}); return res}
