"use client";
import {useEffect,useState} from"react";import AppShell from"@/components/AppShell";
type User={id:string;fullName:string;role:string};type Message={id:string;senderId:string;receiverId:string;body:string;createdAt:string};
export default function Chat(){const [users,setUsers]=useState<User[]>([]),[other,setOther]=useState(""),[messages,setMessages]=useState<Message[]>([]),[body,setBody]=useState(""),[status,setStatus]=useState("");useEffect(()=>{fetch('/api/users').then(r=>r.json()).then(d=>setUsers(Array.isArray(d)?d:[]));},[]);useEffect(()=>{if(!other)return;const load=()=>fetch('/api/messages?with='+encodeURIComponent(other)).then(r=>r.json()).then(d=>setMessages(Array.isArray(d)?d:[]));load();const id=setInterval(load,3000);return()=>clearInterval(id)},[other]);async function send(){if(!other||!body.trim())return;const r=await fetch('/api/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({receiverId:other,body})});const d=await r.json();if(r.ok){setBody('');setMessages(m=>[...m,d]);}else setStatus(d.error||'Message failed');}
const active=users.find(u=>u.id===other);return <AppShell title="Messages" subtitle="Connect and collaborate with people on LeonardX."><div className="chat-layout"><aside><div className="chat-sidebar-head"><h2>People</h2><span>{users.length}</span></div><div className="people-list">{users.map(u=><button className={other===u.id?'user active-user':'user'} key={u.id} onClick={()=>setOther(u.id)}><span className="person-avatar">{u.fullName.slice(0,1).toUpperCase()}</span><span>{u.fullName}<small>{u.role.toLowerCase()}</small></span></button>)}</div></aside><section className="chat-panel">{other?<><div className="chat-panel-head"><span className="person-avatar">{active?.fullName.slice(0,1).toUpperCase()}</span><div><strong>{active?.fullName}</strong><small>Messages refresh automatically</small></div></div><div className="message-list">{messages.length===0?<p className="chat-empty">No messages yet. Start the conversation.</p>:messages.map(m=><div key={m.id} className="message"><p>{m.body}</p><small>{new Date(m.createdAt).toLocaleString()}</small></div>)}</div><div className="composer"><textarea value={body} onChange={e=>setBody(e.target.value)} placeholder="Write a message..."/><button onClick={send}>Send →</button></div></>:<div className="chat-empty-screen"><span>◌</span><h2>Your conversations, in one place.</h2><p>Select a person from the list to start chatting.</p></div>}{status&&<p className="error">{status}</p>}</section></div></AppShell>}
