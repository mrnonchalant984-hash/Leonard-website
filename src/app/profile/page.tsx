"use client";import{useEffect,useState}from"react";import AppShell from"@/components/AppShell";export default function Profile(){const[d,setD]=useState<any>({skills:[]}),[msg,setMsg]=useState('');useEffect(()=>{fetch('/api/profile').then(r=>r.json()).then(setD)},[]);async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/profile',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)});const x=await r.json();if(r.ok){setD(x);setMsg('Profile saved successfully.')}else setMsg(x.error||'Could not save');}return <AppShell title="Profile & Settings" subtitle="Build a professional LeonardX profile and keep your details up to date."><section className="form-page-card profile-card"><form className="app-form" onSubmit={save}><label>Full name<input value={d.fullName||''} onChange={e=>setD({...d,fullName:e.target.value})}/></label><label>Professional headline<input value={(d.headline!==undefined?d.headline:(d.profile?.headline||''))} onChange={e=>setD({...d,headline:e.target.value})} placeholder="e.g. Product designer and brand strategist"/></label><label>Phone number<input value={d.phone||''} onChange={e=>setD({...d,phone:e.target.value})}/></label><label>Bio<textarea value={d.bio||''} onChange={e=>setD({...d,bio:e.target.value})}/></label><label>Skills <small>Separate skills with commas</small><input value={Array.isArray(d.skills)?d.skills.join(', '):d.skills||''} onChange={e=>setD({...d,skills:e.target.value})}/></label><label>Portfolio URL<input value={d.portfolioUrl||''} onChange={e=>setD({...d,portfolioUrl:e.target.value})}/></label><label>Location<input value={(d.location!==undefined?d.location:(d.profile?.location||''))} onChange={e=>setD({...d,location:e.target.value})}/></label><label>Website<input value={(d.website!==undefined?d.website:(d.profile?.website||''))} onChange={e=>setD({...d,website:e.target.value})}/></label><label>Availability<select value={(d.availability!==undefined?d.availability:(d.profile?.availability||''))} onChange={e=>setD({...d,availability:e.target.value})}><option value="">Select availability</option><option>Available now</option><option>Part-time</option><option>Not available</option></select></label><button>Save profile →</button>{msg&&<p className="status">{msg}</p>}</form></section></AppShell>}
