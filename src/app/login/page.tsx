"use client";
import {useState} from"react";import {useRouter} from"next/navigation";import Link from"next/link";
export default function Login(){const r=useRouter(),[e,setE]=useState(""),[loading,setLoading]=useState(false);async function go(f:FormData){setLoading(true);setE("");const x=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const d=await x.json();setLoading(false);if(x.ok)r.push(d.user.role==='ADMIN'?'/admin':'/dashboard');else setE(d.error||'Login failed')}return <main className="auth-page"><div className="auth-brand"><Link className="brand" href="/"><span className="brand-mark">LX</span>Leonard<span>X</span></Link></div><section className="auth-layout"><div className="auth-aside"><span className="eyebrow">WELCOME BACK</span><h2>Continue building your next opportunity.</h2><p>Manage projects, connect with clients, discover work, and use powerful tools from one workspace.</p><div className="auth-points"><span>✓ Built for freelancers and clients</span><span>✓ Clean, focused workspace</span><span>✓ LeonardX AI premium tools</span></div></div><div className="auth-card"><Link href="/" className="back">← Back to LeonardX</Link><h1>Welcome back</h1><p>Log in to continue to your LeonardX workspace.</p><form action={go} className="app-form"><label>Email address<input name="email" type="email" placeholder="you@example.com" required/></label><label>Password<input name="password" type="password" placeholder="Enter your password" required/></label><button disabled={loading}>{loading?'Logging in...':'Log in →'}</button>{e&&<p className="error">{e}</p>}</form><p className="auth-footer">New here? <Link href="/signup?role=FREELANCER">Create Freelancer account</Link> or <Link href="/signup?role=CLIENT">Client account</Link>.</p></div></section></main>}
