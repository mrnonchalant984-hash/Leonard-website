"use client";
import AppShell from "@/components/AppShell";
export default function Post(){async function go(f:FormData){await fetch('/api/jobs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});location.href='/jobs'} return <AppShell title="Post a Job" subtitle="Create a clear project brief and connect with the right talent."><section className="form-page-card"><div className="form-page-intro"><span className="form-icon">＋</span><div><h2>Tell freelancers what you need</h2><p>A detailed job post helps you attract better applications.</p></div></div><form action={go} className="app-form"><label>Job title<input name="title" placeholder="e.g. Build a responsive business website" required/></label><label>Project description<textarea name="description" placeholder="Describe the work, goals, requirements, and expected deliverables..." required/></label><label>Budget (NGN)<input name="budget" type="number" placeholder="Enter your project budget" required/></label><button>Publish Job →</button></form></section></AppShell>}
