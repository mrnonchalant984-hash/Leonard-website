"use client";import{useEffect,useState}from"react";import AppShell from"@/components/AppShell";export default function Transactions(){const[t,setT]=useState<any[]>([]);useEffect(()=>{fetch('/api/transactions').then(r=>r.json()).then(d=>setT(Array.isArray(d)?d:[]))},[]);return <AppShell title="Transactions" subtitle="Track completed work, estimated earnings and LeonardX commission."><div className="transaction-list">{t.map(x=><article className="transaction-card" key={x.id}><div><span className={'payment-status '+x.status.toLowerCase()}>{x.status}</span><h3>{x.job.title}</h3><p>Client: {x.client.fullName} · Freelancer: {x.freelancer.fullName}</p></div><div className="transaction-amounts"><b>₦{x.grossAmount.toLocaleString()}</b><small>Gross project value</small><p>LeonardX fee (15%): ₦{x.commissionAmount.toLocaleString()}</p><strong>Freelancer: ₦{x.freelancerAmount.toLocaleString()}</strong></div></article>)}</div></AppShell>}
