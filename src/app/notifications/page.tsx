"use client";import{useEffect,useState}from"react";import Link from"next/link";import AppShell from"@/components/AppShell";export default function Notifications(){const[n,setN]=useState<any[]>([]);useEffect(()=>{fetch('/api/notifications').then(r=>r.json()).then(d=>setN(Array.isArray(d)?d:[]));fetch('/api/notifications',{method:'PATCH'})},[]);return <AppShell title="Notifications" subtitle="Stay up to date with your work and account activity."><div className="notification-list">{n.length===0?<div className="empty-state"><h2>You're all caught up</h2><p>New activity will appear here.</p></div>:n.map(x=><article className={x.read?'notification-item':'notification-item unread'} key={x.id}><div><span className="notification-type">{x.type}</span><h3>{x.title}</h3><p>{x.body}</p><small>{new Date(x.createdAt).toLocaleString()}</small></div>{x.link&&<Link href={x.link}>Open →</Link>}</article>)}</div></AppShell>}
