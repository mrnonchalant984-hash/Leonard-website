"use client";
import Link from "next/link"; import {useState} from "react";
export default function Contact(){const[msg,setMsg]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMsg("");const f=new FormData(e.currentTarget);const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),email:f.get("email"),subject:f.get("subject"),message:f.get("message")})});const d=await r.json();setMsg(d.message||d.error||"Something went wrong");setLoading(false);if(r.ok)e.currentTarget.reset()}return <main className="contact-page"><header className="contact-header"><Link className="brand" href="/"><span className="brand-mark">LX</span>Leonard<span>X</span></Link><Link className="nav-login" href="/login">Log in</Link></header><section className="contact-card"><div><p className="eyebrow">CONTACT LEONARDX</p><h1>Let’s talk.</h1><p>Have a question, partnership idea, or need support? Send us a message and our team will respond.</p></div><form onSubmit={submit} className="contact-form"><label>Full name<input name="name" required placeholder="Your full name"/></label><label>Email address<input name="email" type="email" required placeholder="you@example.com"/></label><label>Subject<input name="subject" required placeholder="What can we help with?"/></label><label>Message<textarea name="message" required minLength={10} rows={7} placeholder="Tell us more..."/></label><button disabled={loading}>{loading?"Sending...":"Send message →"}</button>{msg&&<p className="form-message">{msg}</p>}</form></section></main>}
